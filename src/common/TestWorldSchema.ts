export enum Class {
  Circle = "TestWorld:Circle",
  SphereElement = "TestWorld:SphereElement",
}

/** The CodeSpec names associated with RobotWorld. */
export enum CodeSpec {
  Circle = "TestWorld:Circle",
  SphereElement = "TestWorld:SphereElement",
}

/** The SpatialCategory names associated with RobotWorld. */
export enum Category {
  Circle = "TestWorld:Circle",
  SphereElement = "TestWorld:SphereElement",
}